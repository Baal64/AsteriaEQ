#pragma once

namespace asteria::config::axisLimits
{

    constexpr float RIGHT_ASCENSION_MIN_DEG = -170.0F;
    constexpr float RIGHT_ASCENSION_MAX_DEG = 170.0F;

    constexpr float DECLINATION_MIN_DEG = -170.0F;
    constexpr float DECLINATION_MAX_DEG = 170.0F;

} // namespace asteria::config::axisLimits